# URL of the running indi-allsky Flask backend.
# Vite dev server proxies /indi-allsky/* to this target.
# If Vite runs on the same Pi as the Flask backend: use https://localhost
# If Vite runs on a different machine: use https://<pi-hostname>
VITE_API_TARGET=https://localhost
